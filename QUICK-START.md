# 🚀 Быстрый запуск проекта

## Проблема: `ERR_CONNECTION_REFUSED`

Эта ошибка означает, что **API сервер не запущен**. Браузер не может подключиться к `http://localhost:3001`.

## ✅ Решение: Запустить API сервер

### Вариант 1: Запустить только API сервер

Откройте новый терминал и выполните:

```bash
cd WhiteShop
npm run dev:api
```

Или:

```bash
cd WhiteShop/apps/api
npm run dev
```

### Вариант 2: Запустить оба сервера одновременно (API + Web)

Из корня проекта:

```bash
cd WhiteShop
npm run dev
```

Это запустит:
- API сервер на `http://localhost:3001`
- Web сервер на `http://localhost:3000` (или другой порт, если занят)

## 📋 Проверка

После запуска сервера вы должны увидеть в консоли:

```
🚀 API Server running on http://localhost:3001
```

Проверьте доступность сервера:

```bash
curl http://localhost:3001/health
```

Или откройте в браузере: `http://localhost:3001/health`

## ⚠️ Важные требования

Перед запуском убедитесь, что:

1. **MongoDB запущен** ⚠️ **ОБЯЗАТЕЛЬНО!**
   - См. инструкцию: [START-MONGODB.md](./START-MONGODB.md)
   - Самый простой способ: `docker run -d -p 27017:27017 --name mongodb mongo:latest`
2. **Redis запущен** (если используется)
3. **Создан файл `.env`** в корне проекта с необходимыми переменными

## 🗄️ Запуск MongoDB (ОБЯЗАТЕЛЬНО!)

**API сервер не запустится без MongoDB!**

### Самый простой способ: Использовать скрипт

**Windows PowerShell:**
```powershell
.\start-mongodb.ps1
```

**Windows CMD:**
```cmd
start-mongodb.bat
```

Скрипт автоматически проверит Docker и запустит MongoDB.

### Или вручную (Docker):

```bash
# Запустить MongoDB в Docker
docker run -d -p 27017:27017 --name mongodb mongo:latest

# Если контейнер уже существует, просто запустите его:
docker start mongodb
```

### Или через Windows Service:

```powershell
net start MongoDB
```

**Подробная инструкция:** [START-MONGODB.md](./START-MONGODB.md)  
**Решение проблем:** [FIX-CONNECTION-REFUSED.md](./FIX-CONNECTION-REFUSED.md)

## 🔧 Если порт 3001 занят

Если порт 3001 уже занят другим процессом:

1. Найдите процесс:
   ```bash
   netstat -ano | findstr :3001
   ```

2. Остановите процесс:
   ```bash
   taskkill /PID <PID> /F
   ```

3. Или измените порт в `.env`:
   ```
   PORT=3002
   ```

   И обновите `NEXT_PUBLIC_API_URL` в `.env`:
   ```
   NEXT_PUBLIC_API_URL=http://localhost:3002
   ```

## 📝 Структура проекта

```
WhiteShop/
├── apps/
│   ├── api/          # API сервер (порт 3001)
│   └── web/          # Next.js приложение (порт 3000)
└── package.json      # Корневой package.json
```

## 🐛 Troubleshooting

### Сервер не запускается

1. Проверьте, что все зависимости установлены:
   ```bash
   npm install
   ```

2. Проверьте логи ошибок в консоли

3. Убедитесь, что MongoDB и Redis доступны (если используются)

### CORS ошибки

CORS уже настроен в `apps/api/src/server.js`. Если возникают проблемы:

1. Проверьте, что `NEXT_PUBLIC_API_URL` в `.env` соответствует адресу API сервера
2. Убедитесь, что web сервер запущен на порту, разрешенном в CORS настройках

